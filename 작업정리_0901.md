# Dayflow — 오늘 작업 정리 (2026-09-01)

버전: 0.35.1 → 0.37.2 (main 브랜치, 전부 커밋+push 완료)

---

## 1. 고객 리마인더 마무리
- 연락처 입력 시 자동 하이픈(010-0000-0000)
- 팝업 예시 placeholder("김땡땡"/"NX300") 제거
- 저장 안 되던 버그 원인 = Firestore 규칙 미반영 → README에 규칙 추가

## 2. AI 어시스턴트 버그 수정
- "폴스타 견적기"처럼 동사 없이 파일명만 말하면 검색 안 하고 "없다"고 잘못 답하던 문제 → 시스템 프롬프트 수정

## 3. 조직 관리 기능 신규 구축 (제일 큰 작업)
- 본사 1~4본부 / 양주·다산·포천지점 구조 + 팀 생성/관리
- 직급(주임~본부장/지점장)과 시스템 권한(superAdmin/orgManager/teamManager/member) 완전 분리
- 사이드바 "조직 관리"(최고관리자) / "우리 조직"(본부장·지점장·팀장)
- 카드형 조직도(클릭해서 배정/수정), 공석 표시, 미배정 계정 목록
- 관리자용 "조직 장부" — 담당 범위 직원들 이번 달 출고건/수수료 조회 전용
- 직원이 다른 본부로 이동해도 새 관리자가 과거 장부까지 볼 수 있게 설계(현재 소속 기준 판단, 데이터 이관 불필요)
- 인사이동/변경 이력 로그(최고관리자 전용)
- 개인별 "링크를 열 때 쓸 브라우저" 설정(Chrome/Edge/Whale/Firefox)
- 타이틀바 "본부시트" 버튼이 소속별로 다른 링크로 열리게(설정에서 본부/지점별 URL 등록)

### 발견해서 고친 버그들
- 재로그인할 때마다 소속/직급/권한이 초기화되던 문제 (가장 심각했던 버그)
- 팀장을 배정해도 계속 "팀장 공석"으로 보이던 문제
- 조직 장부 진입 시 오류 — Firestore 색인 문제 + 직급별 수수료 계산 오류(모바일 브랜치에만 있는 모듈을 잘못 참조)
- "본부시트" 버튼이 로그인 초기엔 옛날 고정 라벨로 뜨던 문제(소속 정보 도착 전에 그려짐) → 소속 정보 오면 자동 재렌더링

## 4. 계정 메모 (상호명/ID/PW) 신규 기능
- 같은 본부/지점 소속끼리만 공유(다른 소속엔 안 보임)
- 암호화는 아직 없음(평문) — Blaze/Cloud Functions 준비되면 나중에 암호화 구조로 전환 예정, 대신 접근 범위를 Firestore 규칙으로 최대한 좁혀둠
- ID/PW 복사 버튼, 담당자 이름 직접 수정 가능
- UI를 상호명별 카드 + flex 흐름 배치로 재설계 — 값이 잘리거나 줄바꿈되지 않고 카드가 옆으로 늘어나는 방식

---

## 아직 안 끝난 것 / 확인 필요

- **financeCredentials 규칙 최신본을 콘솔에 게시했는지 재확인 필요**(README 최신 버전 기준)
- 인사이동/변경 이력 로그 — 만들었지만 실제로 눌러서 확인 안 해봄
- 다른 사람을 팀장/본부장으로 배정했을 때 실제 장부 조회 권한 — 재광님 혼자뿐이라 검증 못함
- 🔐 금융사 계정 공유(관리자 카드, 암호화, 접근 로그) — Blaze 요금제 필요해서 통째로 보류
- 드래그 앤 드롭 직원 배치 — 아직 카드 클릭 방식만

## 참고
- Firestore 보안 규칙 최신본은 항상 `README.md` "1. 준비물" 섹션에 있음 — 바뀔 때마다 콘솔에 다시 게시 필요
- 배포는 파워셀에서 `$env:GH_TOKEN="..."` 설정 후 `npm run publish`
