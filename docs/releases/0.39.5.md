# 여백 PC 0.39.5 · GPT-5.6 Terra

장부 AI 채우기와 AI 어시스턴트의 모델을 `gpt-4o-mini`에서 `gpt-5.6-terra`로 변경했습니다. 기존 장부·캘린더 데이터, 정산 계산과 도구 실행 흐름은 유지합니다.

- 두 기능 모두 GPT-5.6 Terra로 요청합니다.
- Chat Completions를 유지하고 `reasoning_effort: none`을 사용하며 기존 temperature 설정은 제거했습니다.
- 기존 PC의 로컬 API 키를 유지합니다. 공개 설치 파일에 OpenAI 비밀 키를 포함하지 않으며, 처음 설치하는 PC는 별도 로컬 키 설정이 필요합니다.
- 이전 0.39.4의 장부 입력 패널·월 목표·DB 유형·입력 반응성·글씨 확대 개선을 포함합니다.

검증: 실제 API 키의 모델 접근 확인, 실제 JSON 응답·도구 호출 응답 검사, 모의 요청 계약 검사, Node 38개 통과. API 검사는 가상 데이터만 사용했고 실제 일정이나 장부를 생성하지 않았습니다. 이 PC 설치 파일과 빌드 ASAR 일치를 확인했습니다.

설치 파일: `team-notice-app-setup-0.39.5.exe`

SHA256: `3679bc823b4165746f53927d36791407e6ec22130193100e0f02f8c91806a1c0`
