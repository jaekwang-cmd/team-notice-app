# Dayflow — 조직/본부 관리 기능 개발 요청 정리

작성일: 2026-08-31 · 현재 버전: 0.36.2 (main 브랜치)

이 문서는 개발자에게 그대로 전달하는 용도입니다. 1번은 기존 프로그램 현황, 2번이 오늘
새로 요청한 기능 스펙입니다.

---

## 1. 기존 프로그램 현황

### 뭐 하는 프로그램인지
**Dayflow**(내부명 `team-notice-app`)는 자동차 금융/영업직 1인(정재광, 차장→관리자로 승진)이
쓰는 **Windows 데스크톱 앱**(Electron). 팀원들도 각자 자기 PC에 설치해서 각자 로그인해서 씀.

### 현재 기능 (전부 완성/운영 중)
| 기능 | 설명 |
|---|---|
| 캘린더 | 개인 Google Calendar 연동 + 팀 공유 일정(Firestore, 전 직원 공통) |
| 메모장(할일) | Google Tasks 연동, 알람 |
| 고객 리마인더 | 몇 달 뒤 다시 연락할 고객 등록 → 그날 오전 9시부터 자정까지 1시간마다 알림(완료 체크 전까지 반복) |
| AI 어시스턴트 | 자연어로 일정/메모 추가, PC 파일 검색 (OpenAI GPT-4o-mini) |
| 출고 관리 장부 | 자동차 출고 건별 수수료/정산 계산 + 엑셀(출고현황/정산서) 자동 생성. **본인 것만** 보임 |
| 금융사 비교시트 | 여러 금융사 조건 비교 계산기 (로컬 저장, 개인용) |
| 설정 | 테마, 자동실행, 관리자 관리, 계정 삭제 |
| 타이틀바 바로가기 | "어드민", "3본부시트", "카판", "에이원카페" 등 고정 버튼 — 클릭하면 시스템 기본 브라우저로 그 URL이 열림 |

### 로그인/데이터 구조
- 로그인은 Google 계정 하나로 함. 이 Google 로그인이 곧 Firebase Auth 로그인도 됨(캘린더
  접근 권한 + Firestore 쓰기 권한이 동시에 부여)
- **팀 일정(teamEvents)**: 전 직원 공통 공유, 로그인만 하면 전원이 다 봄
- **고객 리마인더 / 출고 장부(chulgoEntries)**: 지금은 완전히 개인 전용 — Firestore 보안
  규칙상 `authorUid`(작성자 본인)만 읽고 쓸 수 있음. 다른 사람 데이터는 관리자를 포함해
  **아무도 못 봄** (이번 요청으로 바뀌는 부분)
- **관리자(admin) 개념**: 지금도 있음 — `settings/admins` 문서에 이메일 목록 저장, 앱 설정
  화면에서 "관리자 관리" 섹션으로 추가/삭제. 관리자는 팀 일정 등에서 남의 글도 수정/삭제
  가능하지만, **개인 장부/리마인더는 관리자도 못 봄** (지금까지는)

### 기술 스택
Electron 32 + Node.js · Firebase(Firestore, Auth) · Google Calendar/Tasks API ·
OpenAI(GPT-4o-mini) · exceljs(엑셀 생성) · GitHub Releases 기반 자동 업데이트

### 진행 중이던 다른 작업 (오늘 요청과는 별개, 참고용)
모바일(Android, Capacitor) 버전을 `mobile-capacitor`라는 별도 git 브랜치에서 준비 중이었고,
현재는 일시 중단 상태. 오늘 요청 작업은 **PC 버전(main 브랜치)**에 대한 것이고 모바일
작업과는 무관함.

---

## 2. 오늘 새로 요청한 기능 — 조직(본부/지점) 관리

### 배경
재광님이 승진하면서 **회사의 직접 관리자**가 됨. 지금까지는 이 앱을 쓰는 사람들이 전부
개별로만 존재했는데, 이제 **본부/지점 단위 조직 구조**를 앱 안에 반영하고 싶어함.

### 조직 구조 (7개 소속 단위, 고정값)
```
본사 — 1본부, 2본부, 3본부, 4본부
양주지점
다산지점
포천지점
```

### 요청 기능 상세

**(1) 계정 ↔ 실사용자 매핑**
- 이 앱을 쓰는 각 Google 계정(이메일)마다, 실제 담당자의 **이름 + 직책**을 관리자가 직접
  입력해서 붙일 수 있어야 함
- 예: `구글 계정: xxxxx@gmail.com` ↔ `실 담당자: 김ㅇㅇ 차장`
- 이메일 주소만으로는 누가 누군지 구분이 안 되기 때문

**(2) 소속 배정**
- 위 각 계정을 7개 소속 단위 중 하나로 배정
- **배정 권한은 재광님(최고관리자)만 가짐** — 본부 관리자들도 남을 배정할 수는 없음

**(3) 본부/지점별 관리자("실 관리자")**
- 배정할 때 특정 계정을 그 소속의 "관리자"로도 지정할 수 있어야 함 (예: 3본부 관리자 지정)
- 이 소속 관리자는 재광님이 아닌 **다른 사람**(각 본부/지점 담당자)이 됨

**(4) 관리자가 보는 범위 — 장부 내용까지 공개 (확정)**
- 본부/지점 관리자는 **자기 소속 직원들의 출고 장부 내용(수수료/정산 포함)까지** 볼 수 있어야 함
- 예: 3본부 관리자는 3본부 소속 전 직원의 이달 출고건/수수료 합계를 볼 수 있음
- 단, 다른 본부/지점 데이터는 못 봄(자기 소속만)
- 이건 지금 없는 기능이라 새로 만들어야 함(지금은 개인 장부가 100% 비공개)

**(5) 본부/지점별 외부 시트 링크**
- 타이틀바의 "본부시트" 같은 버튼이 지금은 고정된 하나의 URL로 가는데, 이걸 **로그인한
  계정의 소속에 따라 다른 URL**로 열리게 바꾸고 싶음
- 예: 3본부로 배정된 계정이 그 버튼을 누르면 3본부 전용 구글시트가 열리고, 양주로 배정된
  계정이 누르면 양주 전용 구글시트가 열림
- 이 소속별 URL 목록도 재광님이 설정할 수 있어야 함

**(6) 개인별 기본 브라우저 설정**
- 지금은 링크를 누르면 Windows 시스템 기본 브라우저로 열림
- 직원마다 쓰는 브라우저가 다름(엣지/크롬 등) — 각자 앱 설정에서 "링크를 열 때 쓸 브라우저"를
  직접 고를 수 있게 해달라는 요청. 이건 조직 기능과는 별개의, 순수 개인 설정 항목

### 확인된 결정 사항 (재확인용)
- 소속 배정 권한: **재광님만** (질문으로 확정)
- 관리자의 장부 열람 범위: **장부 내용까지 공개** (질문으로 확정)

### 아직 구현 안 된 것 (오늘 대화에서 설계만 논의, 코드 작업 시작 전)
- Firestore에 `orgMembers`(계정-소속-이름 매핑), `settings/branchLinks`(소속-URL 매핑)
  새 컬렉션/문서 필요
- 출고 장부 항목에 작성 당시 소속(`branch`)을 같이 저장해야 관리자 열람 규칙을 걸 수 있음
- Firestore 보안 규칙 확장 필요 (본인 것 + "내 소속 관리자"는 읽기 허용)
- 새 화면: 소속 관리자용 "본부 장부 보기" (팀원 목록 + 각자 장부 열람)
- 설정 화면에 "구성원 관리"(재광님 전용) + "기본 브라우저" 섹션 추가
